// GameInput.cpp
//
// Portable game-input contract source stub.
//
// The IInputManager interface is pure virtual; this translation unit exists
// so the portable input contract has a buildable object file in projects
// that do not yet link a platform implementation. Platform-specific
// implementations (e.g. Lib_Engine/DxCommon/DxInputDevice) provide the
// concrete behaviour.

#include "GameInput.h"

namespace GameInput
{
    // Intentionally empty — IInputManager is pure virtual.
}